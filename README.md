train.py creates the ai model and stores them in the models folder, the models folder contains the .py, the ONNX and the optimized ONNX file (newmodel_int8.onnx),infer_onnx.py 
shows the AI's weighing for each possibility (aka his prediction) as well as the true case to compare his prediction to
model_tester.py shows his accuarcy when it comes to predicting/detecting each case.
